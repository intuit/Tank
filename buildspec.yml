version: 0.2

env:
  variables:
    MAVEN_OPTS: "-Xms1g -Xmx2g"
    SKIP_METHODTIMER_TEST: true
    SKIP_GUI_TEST: true

phases:
  install:
    runtime-versions:
      java: corretto25
  build:
    commands:
      - java -version
      # Run the complete build with both unit and integration tests
      - mvn clean install surefire-report:report -P release
      - export PROJECT_VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)
      - mkdir -p artifacts/$PROJECT_VERSION
      - cp web/web_ui/target/tank.war artifacts/$PROJECT_VERSION/
      - cp agent/agent_startup_pkg/target/agent-startup-pkg.zip artifacts/$PROJECT_VERSION/
      - zip -j artifacts/$PROJECT_VERSION/TankZip.zip web/web_ui/target/tank.war agent/agent_startup_pkg/target/agent-startup-pkg.zip
      - |
        if [[ "$PROJECT_VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
          aws ssm put-parameter --name /tank/latest-version --value "$PROJECT_VERSION" --type String --overwrite
          echo "Published version $PROJECT_VERSION to SSM /tank/latest-version"
        else
          echo "Skipping SSM update for SNAPSHOT version $PROJECT_VERSION"
        fi

reports:
  UnitTestReports:
    files:
      - '**/target/surefire-reports/TEST-*.xml'
      - '**/target/surefire-reports/*.txt'
    base-directory: '.'
  IntegrationTestReports:
    files:
      - '**/target/failsafe-reports/TEST-*.xml'
      - '**/target/failsafe-reports/*.txt'
    base-directory: '.'

artifacts:
  files:
    - '**/*'
  base-directory: artifacts
  discard-paths: no

cache:
  paths:
    - '/root/.m2/**/*'
